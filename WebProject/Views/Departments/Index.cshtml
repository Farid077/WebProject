@model List<DepartmentManagementVM>

@{
    ViewData["Title"] = "Departments";
    ViewData["ActivePage"] = "Departments";
}

<div class="topbar">
    <div class="page-title">Issue Departments</div>
</div>

<div class="stats-grid">
    <div class="stat-card" style="--accent-color: var(--accent)">
        <div class="stat-icon">◈</div>
        <div class="stat-label">Total Departments</div>
        <div class="stat-value">@Model.Count()</div>
    </div>
</div>

<div class="section-header">
    <div class="section-title">Department Management</div>
    <a style="text-decoration:none" asp-action="Create" class="btn-accent">+ New Department</a>
</div>

<div class="table-wrap">
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Roles</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var Department in Model)
            {
                <tr>
                    <td>
                        <div class="cell-Issue">
                            @* <div class="mini-avatar" style="background:rgba(200,255,62,.15);color:var(--accent)">
                                @Issue.ame.ToUpper()[0]@Issue.Issuename.ToUpper()[1]
                            </div> *@
                            <div>
                                <div class="cell-name">@Department.Name</div>
                            </div>
                        </div>
                    </td>
                    @* <td><span class="role-tag">@issue.Subtitle</span></td> *@
                    <td>
                        @foreach (var role in Department.Roles)
                        {
                            <div>@role</div>
                        }
                    </td>
                    @* <td>-</td> *@
                    @* <td>@issue.CreatedTime</td> *@
                    <td style="display:flex;">
                        <a asp-action="Update" asp-route-id="@Department.Id" class="action-btn edit" style="text-decoration:none;">Edit</a>
                        <form method="POST" class="action" asp-action="Delete">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@Department.Id" />
                            <button class="action-btn del" type="submit">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
